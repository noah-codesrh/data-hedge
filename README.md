# @data

Hedge protocol dashboard. This is its own app, not a route on the trading site.

```bash
pnpm install
pnpm dev
```

Runs at `http://localhost:5174`. Local reads pick up Supabase keys from `frontend/.env` when this folder has no `.env` of its own.
